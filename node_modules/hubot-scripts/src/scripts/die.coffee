# Description:
#   Tells hubot to close()
#
# Commands:
#   hubot die - Calls hubot's .close() method
#
# URLS:
#   /hubot/die
#
# Notes:
#   These commands are grabbed from comment blocks at the top of each file.

last_words = "Goodbye, cruel world!"

module.exports = (robot) ->
  robot.respond /die/i, (msg) ->
    msg.send last_words
    robot.close()

  robot.router.get '/hubot/die', (req, res) ->
    res.end last_words
    robot.close()
